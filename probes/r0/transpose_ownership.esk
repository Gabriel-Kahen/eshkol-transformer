(define original (reshape #(1.0 2.0 3.0 4.0) 2 2))
(define transposed (transpose original))
(tensor-set! transposed 0 1 9.0)
(display
  (if (= (tensor-get original 1 0) 9.0)
      "TRANSPOSE_ALIASES_STORAGE" "TRANSPOSE_COPIES_STORAGE"))
(newline)
(display "R0_DONE transpose_ownership") (newline)
