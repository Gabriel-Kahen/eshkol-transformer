# G3-C4 Step 23C: first internal manual role

This isolated prerequisite starts from `cf970723c93ff61ef5f82db29313393b33e20dd2`. It adds a translation-unit-private numerical step for ordinal 0, token embedding, after the accepted manual frame begin. No Eshkol entry, accepted `role_step` boundary, public result, frame prepare, commit, or publication is added. The 21-role inventory remains incomplete.

The frame owns an ET scratch slot of actual `[1,T,4]` shape, with storage for T≤2. An authenticated active manual frame with next ordinal 0, pending kind-3 logits, and idle committed cache may dispatch exactly one token-embedding row: G3-N for T1 prefill or manual decode, N3K for T2 prefill. The source-private helper takes IDs only from the frame's owned copy and the pinned token embedding table. It dispatches into a local candidate, then copies into frame scratch and advances next ordinal to 1 only on success. A provider rejection leaves the ordinal and ET slot unchanged and permits retry. Repeated role 0 rejects. The pending logits tensor, committed cache, binding, and RNG remain untouched; abort frees the frame scratch with the transcript.

The next bounded seam is ordinal 1 and the remaining fixed rows, with frame-owned scratch for each actual T shape. Attention ordinal 10 additionally needs a capacity-2 candidate cache and A2 transaction that can be aborted without changing the committed cache. Existing whole-schedule T1/T2/decode helpers use stack scratch, G3-C4 position embedding, and capacity-4 caches, so they cannot be substituted for the accepted per-role route. Later work must complete all 21 ordinals before exposing `role_step`.
