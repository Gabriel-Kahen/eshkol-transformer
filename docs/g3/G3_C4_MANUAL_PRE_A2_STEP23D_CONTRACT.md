# G3-C4 Step 23D: internal manual roles 1–9

This isolated prerequisite starts from `67c579e62124b89c90ccba38356f59e7fc315c42`. It extends the translation-unit-private manual numerical seam from ordinal 0 through ordinal 9 for prefill T1/T2 and manual decode. No Eshkol entry, accepted `role_step` boundary, public result, frame prepare, commit, or publication is added. The 21-role route remains incomplete.

The frame owns actual-T ET, EP, X, N1, QT, KT, VT, QH, KH and VH slots, each with capacity for T≤2. Each helper call authenticates the active frame and pending manual logits, requires the exact next ordinal, and checks the committed cache is idle. It dispatches exactly one fixed provider row into a local candidate, copies only that role's output into its frame slot, then advances the ordinal. A provider rejection leaves the ordinal and destination slot unchanged and permits retry. Wrong, repeated, and out-of-order ordinals reject before dispatch. The committed cache, binding, pending logits and RNG remain untouched; abort frees the owned scratch with the frame.

Ordinal 1 uses G3-N `[1,1,2,4]` for T1/decode or N3K `[1,2,2,4]` for T2. It supplies prefill positions 0/1 or decode position 1 and a bounded read-only `[2,4]` prefix view of the pinned `[4,4]` position table. Ordinal 2 uses G3-N/N3K residual; 3 uses G3-N/N2 norm with epsilon bits `0x3727c5ac`; 4–6 use G3-N/N3K linear with pins 2/0/3; 7–9 use G3-N/N3K head split. All views have the accepted actual T shape and each provider row is fixed by ordinal. The native witness compares slots 0–9 bitwise against the existing T1/T2 whole-forward helpers, despite their different position-table route, and injects one failure at each new ordinal on every manual frame kind.

Attention ordinal 10 is the next seam. It needs a frame-owned capacity-2 candidate cache and A2 transaction with staged K/V and a nested view that is always ended before returning. Decode must copy its committed length-1 prefix into that candidate so failure and abort leave the committed cache untouched. Existing whole-schedule helpers use stack scratch, G3-C4 position embedding, and capacity-4 cache, and cannot be substituted for the accepted per-role route. Complete all 21 ordinals before exposing `role_step`.
